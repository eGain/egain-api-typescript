/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * This schema contains information about Profile.
 */
export type PublishProfile = {
  /**
   * The ID of the guided help profile.<br>
   *
   * @remarks
   * 1 will always be the **system profile**.
   */
  id: string;
  /**
   * The name of the profile.
   */
  name: string;
};

/** @internal */
export const PublishProfile$inboundSchema: z.ZodType<
  PublishProfile,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  name: z.string(),
});

/** @internal */
export type PublishProfile$Outbound = {
  id: string;
  name: string;
};

/** @internal */
export const PublishProfile$outboundSchema: z.ZodType<
  PublishProfile$Outbound,
  z.ZodTypeDef,
  PublishProfile
> = z.object({
  id: z.string(),
  name: z.string(),
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace PublishProfile$ {
  /** @deprecated use `PublishProfile$inboundSchema` instead. */
  export const inboundSchema = PublishProfile$inboundSchema;
  /** @deprecated use `PublishProfile$outboundSchema` instead. */
  export const outboundSchema = PublishProfile$outboundSchema;
  /** @deprecated use `PublishProfile$Outbound` instead. */
  export type Outbound = PublishProfile$Outbound;
}

export function publishProfileToJSON(publishProfile: PublishProfile): string {
  return JSON.stringify(PublishProfile$outboundSchema.parse(publishProfile));
}

export function publishProfileFromJSON(
  jsonString: string,
): SafeParseResult<PublishProfile, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublishProfile$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublishProfile' from JSON`,
  );
}
