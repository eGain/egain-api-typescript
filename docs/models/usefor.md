# UseFor

Specifies which console the prompt applies to: 'knowledge', 'advisor', or 'custom' (for prompts used outside any console).

## Example Usage

```typescript
import { UseFor } from "@egain/egain-api-typescript/models";

let value: UseFor = "custom";
```

## Values

```typescript
"knowledge" | "advisor" | "custom"
```