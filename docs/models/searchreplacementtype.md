# SearchReplacementType

Identifies this replacement as a **search result**. At execution, results from a portal search will replace the variable `name` in the prompt template.

## Example Usage

```typescript
import { SearchReplacementType } from "@egain/egain-api-typescript/models";

let value: SearchReplacementType = "search";
```

## Values

```typescript
"search"
```